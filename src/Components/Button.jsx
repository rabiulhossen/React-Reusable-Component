const Button = ({name}) => {

     const styles ={
          colour: "red",
          background: "green",
          padding: "10px"
     }
   return (
      <div>
         {name}
      </div>
   );
};

export default Button;
